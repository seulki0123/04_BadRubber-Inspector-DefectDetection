# Grade Selection-driven configuration for standalone DefectDetection runs.
# The `production_information` block selects the foundation profile by
# convention -- `src/Profiles/profiles/products/<LINE>/_<grade>.py`. That module
# supplies all classes/checkpoints/thresholds/`return_mode`/`show`; there is no
# registry or override layer. `checkpoint_root` is the single absolute path
# that every checkpoint inside a base profile is anchored to.

checkpoint_root: /home/s1k2/04_BadRubber/nas/NAS/sub/LG_Chemistry/inspection

production_information:
  line: BR-C
  grade: BR1280
